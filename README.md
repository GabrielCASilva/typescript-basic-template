# An Node Typescript basic template

## important commands to know

#### tsconfig.json
npx tsc --init

#### create an basic jest config file
```bash
  npx ts-jest config:init
```

TODO: rever eslit
install husky
  - pre-commit lint / formatter / prerier / test
